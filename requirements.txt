pandas>=2.0
openpyxl>=3.1
xlrd>=2.0
pdfplumber>=0.11
click>=8.0
rich>=13.0
python-dotenv>=1.0
sqlalchemy>=2.0
# Esquemas del Sistema Presupuestal (app/presupuesto)
pydantic>=2.6
flask>=3.0
flask-wtf>=1.2
waitress>=3.0
gunicorn>=21.0
python-dateutil>=2.8
requests>=2.31
azure-storage-blob>=12.0
# Telemetría de Application Insights (Fase 4). Solo se activa cuando
# APPLICATIONINSIGHTS_CONNECTION_STRING está definida (ver app/web/__init__.py).
azure-monitor-opentelemetry>=1.6
fsspec>=2023.0
# Solo se importa cuando USE_SQLITE=false (Azure SQL). Se incluye aquí para que
# quede empaquetado en ./vendor durante el despliegue y no haya que descargarlo
# en cada arranque del contenedor (ver startup.sh / application.py).
pyodbc>=5.1
